# URL of the deployed backend API (include /api suffix)
VITE_API_URL=http://localhost:5000/api

# Base URL of the deployed backend (no /api suffix — used for the Socket.IO connection)
VITE_SOCKET_URL=http://localhost:5000
